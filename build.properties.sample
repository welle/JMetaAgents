# Defines where the jars are to be built
# jars_dest = d:/projets java/jmetaagents
jars_dest = 

# Defines where the javadoc is to be built
# Only required if building Javadoc via Ant
# javadoc_dest = d:/projets java/jmetaagents/javadoc
javadoc_dest = 

# Defines the version
# version = 1.5beta
version = 

